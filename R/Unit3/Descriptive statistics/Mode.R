# The value that occurs most frequently in the data set.

#types of mode:
  #1. uni model
  #2. bi 
  #3. tri model
  #4. multi model

v = c(1, 4, 2, 1, 4, 5, 6, 3, 2, 1, 3, 4, 5, 9)

# no built in method for mode. We need to implement it manually.